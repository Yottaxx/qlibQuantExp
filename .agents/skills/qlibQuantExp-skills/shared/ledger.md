# Research Ledger

Faraday's Diary + Index for the qlibQuantExp project. Append-only. Schema in `skills/qx-research-ledger/references/ledger_schema.md`.

**Cumulative `kind=sweep_done` count (n_trials):** 0 at file creation.

---

## 2026-05-22

| id | kind | key | context |
|----|------|-----|---------|
| 1 | `anchor` | sha=`3675090` ic_raw=0.0706 rank_ic=0.0786 | macV2 baseline frozen on CSI300 t+5 (`Ref($close,-5)/Ref($close,-1)-1`); 5-seed median; current=true; cites `analysis/night_plan_master.md` §0 |
| 2 | `decision` | adopt `research/macV2-night` with Tier-1 patch set | A1 (temporal evidence readout, 4-arm: last/mean/static_attn/regime_attn) + A3 (weight EMA, decay rampup) + D1–D4 (diagnostics); cites `analysis/night_plan_change_set.md` |
| 3 | `decision` | demote A5 (stable layer_summary) to Tier-2 | measurement-first principle: cannot fix without first measuring chunk inconsistency via D4; cites `analysis/night_plan_dialectic.md` §3 |
| 4 | `decision` | reject B2 (antisymmetric expert init) | self-attack showed it would INCREASE expert correlation rather than decrease — opposite of intent; cites `analysis/night_plan_dialectic.md` §5.8 |
| 5 | `decision` | harden A1 ablation from 2-arm to 4-arm | force head-to-head: `last_step` vs `mean_t` vs `static_attn` vs `regime_attn` to attribute regime-conditioning cost separately from attention-pooling cost; cites `analysis/night_plan_dialectic.md` §1 |
| 6 | `decision` | macV3 hierarchical state field set to `status=deferred` | rejected as monster-barring bundle in `hardcore.md` RF-2026-05-22-01; cites `analysis/macV2_vs_macV3_quant_algorithm_diff.md` |
| 7 | `decision` | install qlibQuantExp-skills v0.1 (6-skill imagination package) | 4 imagination + ledger + skill-creator; lineages: Gentner / Popper / Faraday / Lakatos / Anthropic skill-creator / Reflexion; cites `~/.claude/plans/skills-anthropic-skills-sota-skills-sot-greedy-gadget.md` |

---

## 2026-05-23

| id | kind | key | context |
|----|------|-----|---------|
| 8 | `decision` | register qlibQuantExp-skills for Codex + add bottleneck report | package entry + qx junctions + report generated; cites `.agents/skills/qlibQuantExp-skills/SKILL.md`, `analysis/current_bottleneck_report.md` |
| 9 | `decision` | implement deterministic runtime + diagnostic-first bottleneck patch | seed/CUDA warn-only determinism, sampler stats, router oracle, expert/temporal diagnostics; cites `module/model_adapter.py`, `work_flow.py` |

---

## Future entries

Append below using the same column shape (`| id | kind | key | context |`). The `id` field is globally sequential (continue from 10). Group rows under `## YYYY-MM-DD` date headings; create the heading when needed.

The 5 entry kinds: `anchor` / `sweep_start` / `sweep_done` / `result` / `decision`. Use `correction` (with `cites: prior_id`) to fix a wrong prior row — never edit in place.

## 2026-05-23

| id | kind | key | context |
|----|------|-----|---------| 
| 8 | `anchor` | session_entry: 31 MLflow runs surveyed, 23 diagnostic-backed, best single-seed rank_ic=0.0796 (cap00 s43), best setting rank_ic=0.0794 (cap06 s42) | Comprehensive experiment inventory compiled; 4 bottlenecks diagnosed: (1) train-valid gap ~0.10-0.14, (2) time_only ≈ base (+0.0007), (3) learned routing edge not secure vs fixed_0.5, (4) regime-alpha misalignment. n_trials=0 under new framework. |
| 9 | `decision` | 5-phase exploration plan adopted: P1-statistical confirmation (5-seed cap06/full_135/fixed/film), P2-temporal enhancement (context_len + A1 4-arm), P3-regime state enrichment, P4-router calibration, P5-KDD publication readiness | Priority: P1 before any architecture change; Phase 1 acceptance rule: cap06/full must beat fixed_0.5 by ≥0.0015 mean RankIC without RankICIR degradation across 5 seeds. Overall target: rank_ic ≥ 0.085 CSI300 5-seed mean. |

---

## 2026-05-24

| id | kind | key | context |
|----|------|-----|---------|
| 10 | `decision` | skill-based improvement paths selected | Diagnostic-first paths: sampler coverage, router oracle, temporal local evidence, FiLM-pooling coordination, score dispersion; cites `analysis/skill_improvement_paths_20260524.md` |
| 11 | `sweep_start` | diagnostic_full135_det_seed42 | Single deterministic full_135 run, seed=42, sampled_daily; read sampler/router_oracle/expert/temporal metrics; cites `diagnostic_runs/full135_deterministic_20260524` |
| 12 | `decision` | ratify HC-6: four-tuple headline + 5-seed mean±std + one-belt-parameter-at-a-time | Audit of full_135 (seed42, 2026-05-01) vs cap06 (seed42, 2026-05-05) showed Δ RankIC = +0.0003 but Δ IR_with_cost = +0.522 (+33%) and Δ MaxDD = +0.018 (180 bp better). RankIC alone was a misleading headline. Decision metric is now `{daily_rank_ic_mean, IR_with_cost, MaxDD_with_cost, post_peak_decay}` at 5-seed mean±std, one belt parameter at a time. Cites `shared/hardcore.md` Core amendment 2026-05-24; `~/.claude/plans/tidy-sauteeing-pascal.md` Parts A–B. |
| 13 | `result` | τ-collapse root cause located: `tau_mlp_out_scale=0.01` damps MLP output by 100× | `module/architecture/regime_adaptive_embedding.py:62-63,88` — `tau_raw = tau_base + tau_fc2(h) * 0.01`, so per-sample τ contribution is bounded ±0.01 around `softplus(tau_base) + tau_min ≈ 5.0`. Predicted `tau_p90−tau_p10 ≈ 0.02`; observed 0.016 (full_135) / 0.027 (cap06). Comment claims "LayerScale-style, starts small while allowing gradients to flow" but there is no rampup. Detach in `regime_encoder.py:84-92` is benign (blocks gradient from pc1_ratio back to factor matrix, which is correct). Cites `shared/falsifiers/tau-collapse-forensics-20260524.md`, `shared/notebook/2026-05-24.md` Entry 1. |
| 14 | `result` | pre-flight for tau-collapse-forensics-20260524: `tau_mlp_out_scale` NOT plumbed through `QuantMoEConfig` | `quant_moe_model.py:59-67` constructs `RegimeAdaptiveTimeEmbedding` without passing `tau_mlp_out_scale`; falls back to the module-level default `0.01` at `regime_adaptive_embedding.py:37`. Pure-override path via `QIB_MODEL_OVERRIDES_JSON` is BLOCKED. Two paths to test the falsifier: (a) add `time_tau_mlp_out_scale` field to `QuantMoEConfig` and one-line kwarg passthrough at `quant_moe_model.py:59-67` (clean; still default 0.01 — belt addition under HC-6, not structural), or (b) change the module default at `regime_adaptive_embedding.py:37` directly (cruder, affects all runs). Awaiting owner decision before applying. |
| 15 | `result` | τ collapse is four compounding factors, not one bug | (1) `tau_mlp_out_scale=0.01` 100× clamp on per-sample MLP output (proximal cause); (2) `regime_encoder.compute_internal_stats` at `regime_encoder.py:167-176` `stats.expand(B,-1)` broadcasts regime to all stocks in a day — τ varies across days only, never across stocks; (3) 4-dim regime stats on CSI300 have low day-over-day dynamic range (`crowd ≈ ±0.05`, `pc1 ≈ ±0.05`, `tail ≈ ±0.02` in calm periods), further compressed by terminal LayerNorm in encoder; (4) `T=8` (`work_flow.py:72,129` overrides config default 32 → 8) makes the loss surface for τ shallow — even removing clamp leaves weak gradient pressure. Removing only mech-1 predicts `tau_range_utilization` → ~0.03-0.05 (100×), still capped by mechs 2-4. CSI800 should show 2-5× higher native cross-day swing than CSI300 (free check). Cites `shared/notebook/2026-05-24.md` Entry 2. |
| 16 | `result` | τ collapse does NOT cause pooling-attention collapse | `attention_pooling.py::AdaptivePooling` has no regime input and no τ input — architecturally disconnected from `RegimeAdaptiveTimeEmbedding`. Pool collapse (`pool_entropy_norm ≈ 0.99999`, six nines) is a *separate* rank-1 attention collapse: 1-query × N=158 keys with scale `sqrt(D)=8`, no specific gradient pressure to leave uniform. With `pooling_alpha=0.7` blended into mean-pool, α is currently irrelevant (the attention component is itself near-mean). Fixing τ will NOT fix pooling; fixing pooling will NOT fix τ. They must be tracked as INDEPENDENT belt-parameter sweeps (HC-6). Cites `shared/notebook/2026-05-24.md` Entry 3. |
| 17 | `correction` | Mech-3 description in `notebook/2026-05-24.md` Entry 2 was wrong about regime input dimensionality | `model_configuration.py:59` has `use_external_macro: bool = True` as default. All full_135 / cap06 / matrix runs loaded `data/market_state_csi300.pkl` — a **102-dim** macro feature vector per day (7 base stats + 16 PCA × 4 lookback flavors + 10 market-level aggregates). The 4-stat internal path is the fallback, not the default. Upstream signal is therefore much richer than first stated; the binding constraint is the encoder's terminal LayerNorm stripping magnitude, not 4-dim input narrowness. Post-Mech-1 ceiling on `tau_range_utilization` revised upward from `[0.03, 0.10]` to `[0.05, 0.30]`. Cites `notebook/2026-05-24.md` Entry 2 (corrected in place); supersedes row 15's Mech-3 phrasing. |
| 18 | `decision` | Plumb `time_tau_mlp_out_scale` through `QuantMoEConfig` (plan a) | Belt-addition under HC-6 (one parameter, default unchanged at 0.01, no structural change). Added field at `module/utils/model_configuration.py:37, 111`; passthrough kwarg at `module/quant_moe_model.py:66`. Smoke test: default config → `tau_mlp_out_scale=0.01`; override config → `1.0` propagates to `model.time_embedding.tau_mlp_out_scale`. Falsifier `tau-collapse-forensics-20260524.md` arms `mid_gain` (0.1) and `unit_gain` (1.0) now executable via pure `QIB_MODEL_OVERRIDES_JSON` with no further code change. |
| 19 | `sweep_done` | diagnostic_full135_det_seed42_ndrop30 complete | seed=42, deterministic warn, sampled_daily, n_drop=30; run_id=689cd5c9181a4f329bd281d4a5116e49; cites `mlruns/239161070249761302/689cd5c9181a4f329bd281d4a5116e49/diagnostic_matrix.csv` |
| 20 | `result` | RankIC=0.078232 RankICIR=0.572067 oracle_gap=0.019391 | sampler coverage_gap=0.338667 duplicate_rate=0.366382; forced_time_delta=-0.015067; portfolio IR cost=-0.314254 |
| 21 | `result` | τ pre-flight numerical sim (seed 42, synthetic B=300) | `tau_fc2.weight std` at init = 0.023 (HF init, initializer_range=0.02); `fc2(h)` std post-LN→fc1→GELU = 0.0103. Init-time τ spread (p90−p10): scale=0.01 → 0.0002; scale=1.0 → 0.0246. CSI300 macro pkl per-col std median 1.02, day-over-day diff std median 0.90, sufficient upstream variability. Implication: at scale=1.0 the LOWER bound for post-training tau_range_utilization is ~0.02 (init-time alone); UPPER bound ~0.30 requires optimizer to amplify `tau_fc2.weight` magnitude during training. Cites `~/.claude/plans/tidy-sauteeing-pascal.md` Track 1 Phase 0. |
| 22 | `decision` | file pool-forensics-20260524.md (Track 1.5) | 3-arm card investigating six-nines pool entropy collapse: R1 (multi-head pool, needs plumbing), R2 (pooling_alpha=1.0, pure override), R3 (logit-scale temperature, needs code change). Architecturally independent from τ track per notebook Entry 3. Anchored at `attention_pooling.py:25-50, 100-131`, `quant_moe_model.py:105-110`. Status open until sweep complete. |
| 23 | `result` | Phase 0.5 post-training inspect: optimizer DID try to amplify τ — Mech-1 confirmed dominant | Loaded `mlruns/325032212672679181/.../artifacts/model` (full_135 seed-42, 40 epochs) and `mlruns/731153307123711936/.../artifacts/model` (cap06 seed-42). vs fresh init: full_135 `tau_fc2.weight std 0.023→0.064 (2.80×)`, `tau_fc1.w std 0.020→0.034 (1.72×)`, `tau_fc2.bias 0→0.038`, `tau_base 4.489→4.440`. cap06 similar (`tau_fc2.w 2.53×`). **Strong evidence the loss gradient WAS pushing for τ adaptation** but the 0.01 output clamp suppressed expression. Synthetic forward with trained MLP at scale=1.0 → `tau_range_util ≈ 0.012` (sim), real-data ≈3× → `[0.02, 0.05]` corroboration range. Revised P1 threshold: ≥ 0.02 corroborates (was already this). Position embedding `pos` std slightly SHRANK (0.84×) — likely because τ-weighted pos was effectively constant, so model demphasized it. Cites Phase 0.5 of `~/.claude/plans/tidy-sauteeing-pascal.md` Track 1. |

## 2026-05-25

| id | kind | key | context |
|----|------|-----|---------|
| 24 | `decision` | extend `scripts/run_diagnostic_experiments.py` SETTINGS with 4 tau-scale arms | Added `tau_scale_01/10/20/50` to SETTINGS dict (lines 103-156 of script). Each is a copy of `full_135` model overrides + `time_tau_mlp_out_scale` ∈ {0.1, 1.0, 2.0, 5.0} and `train_stop_threshold=None` (overrides the default 1.35 to guarantee 40-epoch completion). Verified via `--print-only` dispatch: all 4 arms accepted, per-arm manifest.json captures correct overrides at `diagnostic_runs/tau_forensics_20260525/<arm>/`. Reviewer note: this is a config addition under HC-6 belt, not structural — pure data, no code-path change. |
| 25 | `sweep_start` | tau_forensics_20260525 — 4-arm Phase-1 τ-collapse sweep dispatched | 4 arms × seed=42 × 40 epochs, base=full_135, scale∈{0.1, 1.0, 2.0, 5.0}, deterministic_mode=warn, sampled_daily, lr=5e-5, batch=300, amp_fp16, market_state_csi300. Sequential ~12 GPU-hr expected wall-time. Output dir: `diagnostic_runs/tau_forensics_20260525/`. Settles falsifier `shared/falsifiers/tau-collapse-forensics-20260524.md` against P1/P2/P3/P4 prohibitions. Cites `~/.claude/plans/tidy-sauteeing-pascal.md` Track 1 Phase 1. |

## 2026-05-27

| id | kind | key | context |
|----|------|-----|---------|
| 26 | `sweep_done` | tau_forensics_20260525 complete | 4/4 arms returncode=0; wall ≈ 32 GPU-hr actual (longer than estimated due to deterministic mode overhead). mlflow runs: scale_01=`0d7c9875`, scale_10=`f170c5e8`, scale_20=`2bdf1d94`, scale_50=`d96fd228`. Suite auto-flags `tau under-adaptive` DISAPPEARED at scale ≥ 1.0 — Mech-1 confirmed dominant cause. |
| 27 | `result` | tau_scale_01 (scale=0.1): tau_range_util=0.00539 (17× baseline); RankIC=0.0820 (+29bp); IR=1.734 (+11%); MaxDD=-0.0602 (+383bp); post_peak_decay=0.0093 ≈; gen-gap=0.1186 ≈; pool_entropy=0.99999 (unchanged); grad_nonfinite=0.00034 (identical). | Best τ↔volatility spearman=-0.364 (most regime-aligned arm). τ p10-p90=[4.94, 5.21]. Router shifted: time_ratio_vs_pc1: -0.08→-0.30, vs corr_mean: -0.15→-0.36. PASS HC-6 4-tuple no-regression. |
| 28 | `result` | tau_scale_10 (scale=1.0): tau_range_util=0.03092 (100× baseline, ≥P1 threshold 0.02); RankIC=0.0805 (+14bp); IR=**1.813 (+16%)**; MaxDD=-0.0617 (+366bp); post_peak_decay=0.0091 (=); gen-gap=0.1202 ≈; pool_entropy=1.00000 (unchanged); grad_nonfinite=0.00034 (=). | τ p10-p90=[4.66, 6.19], tau_std=0.582 (97× baseline). Highest router regime alignment: vs pc1=-0.42, vs corr_mean_abs=-0.48. PASS HC-6. **Strongest-corroborate single arm.** |
| 29 | `result` | tau_scale_20 (scale=2.0): tau_range_util=0.05955 (192×); RankIC=0.0802 (+11bp); IR=1.804 (+15%); MaxDD=-0.0949 (-101bp slight worse); AnnRet=**0.1890 (best, +6.4%)**; post_peak_decay=**0.0076 (best, -16%)**; gen-gap=0.1156 (-25bp best). | τ p10-p90=[3.94, 6.89], tau_drift=-0.027 (small). Surprise: post_peak_decay IMPROVED below baseline despite stronger τ adaptation. PASS HC-6 (MaxDD slight regress within tolerance). |
| 30 | `result` | tau_scale_50 (scale=5.0): tau_range_util=0.11053 (357×); RankIC=**0.0823 (best, +33bp)**; RankICIR=**0.594 (best)**; IR=1.682 (+8%); MaxDD=-0.0776; post_peak_decay=0.0124 (**+37% worse, BORDERLINE P2**); tau_drift=-2.687 (large neg). | τ p10-p90=[3.44, 8.91] — 3.5× memory ratio, genuinely regime-adaptive. But tau_drift large-negative → τ overshoots then retreats; post_peak_decay >+0.003 threshold marginal P2 trigger. Regime alignment weaker than scale=1.0 (router_vs_pc1 -0.28 vs -0.42). |
| 31 | `decision` | Phase 1 verdict: **settled-corroborate-strong**; promote `time_tau_mlp_out_scale=1.0` to Phase 2 multi-seed gate | Mech-1 confirmed as dominant cause (auto-flag `tau under-adaptive` gone at all scales ≥1.0, monotonic +log-linear ramp 0.00031→0.11053). All 5 metric-tier checks pass: (1) monotonicity ✓, (2) useful adaptation ≥0.02 ✓ at scale=1.0, (3) HC-6 4-tuple no-regression ✓ at scale ∈ {0.1, 1.0, 2.0}, (4) AMP stable across all 4 ✓ (grad_nonfinite=3.4e-4 identical), (5) P4 pool independence ✓ (pool_entropy stays 1.000). scale=5.0 marked as upper-limit candidate (RankIC-best but post_peak_decay borderline). scale=1.0 chosen for Phase 2: cleanest HC-6 profile, +16% IR, +366bp MaxDD, strongest router regime alignment. Falsifier card status → `settled-corroborate-strong`. |
| 32 | `result` | gen-gap insensitivity to τ confirmed across 5 configs | rank_ic_gap_last: baseline 0.121 / scale_01 0.119 / scale_10 0.120 / scale_20 0.116 / scale_50 0.122 — invariant within ±2bp despite 357× τ adaptation. Combined with earlier capacity-invariance (cap00→cap06 also unchanged at 0.12), this triangulates: gen-gap is sampler/data-side. Track 4 design (sampler-dedup / EMA-rampup) remains the correct target. |
| 33 | `result` | router regime axes shift consistently with τ adaptation | Router/time_ratio_vs_corr_mean_abs_spearman: baseline -0.149 → scale_01 -0.364 → scale_10 **-0.477** → scale_20 -0.462 → scale_50 -0.333. Router/time_ratio_vs_pc1: -0.08 → -0.30 → **-0.42** → -0.42 → -0.28. Tail axis: -0.31 → -0.26 → -0.24 → -0.24 → -0.29. **Pattern: when τ adapts, router stops relying on tail signal and engages PC1/cross-stock-correlation signals.** Identical pattern previously observed in cap06 (corr=-0.33, pc1=-0.30 at d_model=96). Suggests two regime-conditioning paths (τ-time and router-gate) are *complementary*, not redundant. |
| 34 | `decision` | extend SETTINGS with 4 mid-range τ scale arms before multi-seed | Add `tau_scale_02/05/08/15` (scale ∈ {0.2, 0.5, 0.8, 1.5}) to `scripts/run_diagnostic_experiments.py` SETTINGS (lines 157-208). Motivation: existing 4-arm sweep has wide log-scale gaps {0.01, 0.1, 1.0, 2.0, 5.0}; the best-headline region [0.1, 2.0] is sparse with only 3 points. Filling in 0.2/0.5/0.8/1.5 lets us (i) check whether the four-tuple improvement is monotonic in scale or has a clear optimum, (ii) test whether scale=0.5 already captures most of the IR/MaxDD improvement without 1.0's full τ adaptation, (iii) pick the most parsimonious promotion candidate before committing 24 GPU-hr to multi-seed. Belt-config additions only; no structural change. |
| 35 | `sweep_start` | tau_forensics_refine_20260527 — 4-arm mid-range fill-in dispatched | 4 arms × seed=42 × 40 epochs, base=full_135, scale∈{0.2, 0.5, 0.8, 1.5}, same trainer config as 25 (deterministic warn, sampled_daily, lr=5e-5, batch=300, amp_fp16). Sequential ~30 GPU-hr expected (actual per-arm ≈ 7-8 hr deterministic). Output dir: `diagnostic_runs/tau_forensics_refine_20260527/`. Will be merged with rows 27-30 into an 8-point scale-response analysis before any Phase 2 multi-seed launch. |

## 2026-05-28

| id | kind | key | context |
|----|------|-----|---------|
| 36 | `sweep_done` | tau_forensics_refine_20260527 complete | 4/4 arms returncode=0. mlflow runs: scale_02=`54485e18`, scale_05=`1bf2f8f7`, scale_08=`d81b8836`, scale_15=`204e809c`. Combined with rows 27-30 yields 9-point τ scale-response curve (baseline 0.01 + scales {0.1, 0.2, 0.5, 0.8, 1.0, 1.5, 2.0, 5.0}). Suite-flag `tau under-adaptive` persists at scale=0.2 (boundary), gone at scale ≥ 0.5. |
| 37 | `result` | scale=0.2: tau_range_util=0.00540 (17×, same as scale=0.1); RankIC=0.0803; IR=1.728; MaxDD=-0.072; ppd=0.0097; AnnRet=0.187 | **PLATEAU finding 1**: scale=0.2 produces near-identical tau_range_util to scale=0.1 (0.00540 vs 0.00539) despite 2× input scale. Optimizer likely hit same local minimum for both, OR the response is sub-linear in this regime. RankIC actually slightly *worse* than scale=0.1 (0.0803 vs 0.0820). |
| 38 | `result` | scale=0.5: tau_range_util=0.01773 (57×); RankIC=**0.0813**; **IR=1.859 (BEST overall)**; MaxDD=-0.079; ppd=0.0097; AnnRet=0.183; **router_vs_pc1=-0.20 (WEAKEST among active arms)** | **STRONGEST single-arm IR** across all 9 points. But router regime alignment is unexpectedly WEAK (-0.20 vs scale=1.0's -0.42). Possible interpretation: at scale=0.5 τ adapts just enough to help portfolio without "pulling" the router into PC1/corr engagement — could be a more parsimonious / lower-entropy operating point. Needs multi-seed to confirm IR=1.86 is real and not single-seed luck. |
| 39 | `result` | scale=0.8: tau_range_util=0.02228 (71×); RankIC=0.0803; **IR=1.475 (ANOMALY, −20% vs neighbors)**; AnnRet=**0.151 (WORST among active arms)**; MaxDD=-0.068; ppd=0.0087 | **ANOMALY** — RankIC and tau_util are normal (consistent with monotonic scale curve), but portfolio-level metrics collapse. Sandwiched between healthy scale=0.5 (IR 1.86) and scale=1.0 (IR 1.81). Most likely single-seed bad-luck on specific stress days. **Strong argument for multi-seed**: scale=0.8 single-seed is unrepresentative — exactly the kind of false-signal HC-6 was designed to filter out. |
| 40 | `result` | scale=1.5: tau_range_util=0.03020 (≈ scale=1.0); RankIC=0.0797 (lowest among active arms); IR=1.836; MaxDD=-0.068; ppd=0.0089; tau_drift=**-0.344 (sign flip vs scale=1.0's +0.155)** | **PLATEAU finding 2**: tau_range_util at scale=1.5 (0.0302) ≈ scale=1.0 (0.0309) — virtually identical despite 1.5× input. Suggests optimizer-amplification saturates between 1.0 and 1.5. tau_drift sign flip is interesting: at scale=1.5 the eval-window end shows smaller τ than start; possibly τ on stress periods has converged to the lower portion of its accessible range. |
| 41 | `result` | 9-point scale-response curve characterized | tau_range_util: 0.0003 (s=0.01) → 0.0054 (s=0.1, 0.2) → 0.0177 (s=0.5) → 0.0223 (s=0.8) → 0.0309 (s=1.0, 1.5) → 0.0596 (s=2.0) → 0.1105 (s=5.0). Two plateaus visible (s∈[0.1, 0.2], s∈[1.0, 1.5]) suggest the optimizer-amplification dynamic is step-like, not smooth in scale. Most-monotonic transitions: 0.2→0.5 (3.3×), 0.5→0.8 (1.26×), 0.8→1.0 (1.39×), 1.5→2.0 (1.97×), 2.0→5.0 (1.85×). Saved: `analysis/tau_scale_response_20260528_9pts.png`. |
| 42 | `decision` | Promote to Phase 2: BOTH `scale=0.5` AND `scale=1.0` as co-candidates | Rationale: (a) scale=0.5 has the BEST single-seed IR (1.86) — but with WEAKEST router regime alignment and the scale=0.8 anomaly nearby raises noise concerns; (b) scale=1.0 has strongest router regime alignment (-0.42 vs PC1, -0.48 vs corr) + best MaxDD (-0.062) + middle-of-pack everything else — most theoretically defensible; (c) running both at 5-seed lets us compare 5-seed mean±std and pick the empirical winner under HC-6 (no Tier-S regression). Reject scale=1.5 (lowest RankIC, plateau with scale=1.0, no parsimony argument). Reject scale=2.0 (MaxDD regress). Reject scale=5.0 (post_peak_decay borderline). Phase 2 launch plan: scale_05 × {43,44,45,46} + scale_10 × {43,44,45,46} + baseline (full_135) × {43,44,45,46} = 12 runs, ~90 GPU-hr in deterministic mode. |

| 24 | `decision` | file full-daily-sampler-20260524.md | Pre-registered full_daily sampler falsifier only; no training dispatched yet. Anchor is deterministic sampled_daily run `689cd5c9181a4f329bd281d4a5116e49` with sampler coverage_gap=0.338667, duplicate_rate=0.366382, upsample_day_ratio=0.965760, rank_ic_gap_last=0.081608, RankIC=0.078232. Candidate changes only `trainer_config.train_sampler_mode` to `full_daily`; regime state, full135 architecture, seed policy, loss, and capacity remain fixed. Original draft mentioned n_drop=30 portfolio layer; superseded by row 25. Cites `shared/falsifiers/full-daily-sampler-20260524.md`. |
| 25 | `correction` | full-daily sampler falsifier portfolio layer aligned back to n_drop=5 | User correction: `n_drop` should remain 5 to match previous full_135 portfolio comparisons. Updated `work_flow.py` default `TopkDropoutStrategy.n_drop` back to 5 and amended `shared/falsifiers/full-daily-sampler-20260524.md`. Row 24's deterministic sampled-daily run remains valid only as training/sampler/RankIC diagnostic anchor; its n_drop=30 portfolio metrics are ignored. Candidate Phase A run will use `full135_full_daily_seed42_ndrop5`. |
| 26 | `sweep_start` | full-daily-sampler-20260524 Phase A launched | Started `full135_full_daily_seed42_ndrop5` with seed=42, deterministic warn mode, `train_sampler_mode=full_daily`, `sampler_diag=true`, and portfolio `n_drop=5`. Main process PID=24132. MLflow experiment=837747670911066300, recorder=8ea7cbc13b5346be96d078d6a2a2aece. Logs: `logs/full_daily_sampler_20260524/full135_full_daily_seed42_ndrop5.stdout.log`, `logs/full_daily_sampler_20260524/full135_full_daily_seed42_ndrop5.stderr.log`. |
| 27 | `correction` | aborted first full_daily Phase A launch due to order confound | Code audit found `DailyChunkBatchSampler` used chronological date order in train mode, while `sampled_daily` shuffles date order by seed/epoch. That made row 26 a coverage+curriculum bundle, not a pure sampler falsifier. Stopped PID=24132 before completion. Fixed `module/dataloader/sampler.py` to support `shuffle`, `seed`, `set_epoch`, and native `last_epoch_stats`; updated `module/model_adapter.py` so train `full_daily` passes `shuffle=True` and `seed=random_seed`, while eval/predict remain ordered. Smoke: same seed/epoch same order, different epoch changed order, every sample included exactly once, duplicate_rate=0. |
| 28 | `sweep_start` | full-daily-sampler-20260524 Phase A relaunched after sampler fix | Restarted `full135_full_daily_seed42_ndrop5` after epoch-aware full_daily shuffle fix. Main process PID=29216. MLflow experiment=837747670911066300, recorder=0c27f34670b64e989e736fa144ddc213. Logs: `logs/full_daily_sampler_20260524/full135_full_daily_seed42_ndrop5_rerun.stdout.log`, `logs/full_daily_sampler_20260524/full135_full_daily_seed42_ndrop5_rerun.stderr.log`. |
| 29 | `correction` | aborted row 28 because it still allowed train-loss threshold stop | User requested the paired hook runs must complete all 40 epochs. Row 28 inherited `train_stop_threshold=1.35`, so it could stop at epoch >=5 after two below-threshold epochs. Stopped PID=29216 before completion. Added `scripts/run_full_daily_sampler_paired_hook.py` and amended `shared/falsifiers/full-daily-sampler-20260524.md` so paired hook runs use `n_epochs=40`, `early_stop=0`, `train_stop_threshold=null`, `min_epochs=40`, `consecutive_k=999999`. |
| 30 | `sweep_start` | full-daily-sampler-20260524 paired 40e hook launched | Launched hook driver PID=36384. It runs `full135_full_daily_seed42_ndrop5_40e` first, then sampled/full paired seeds `{42,43,44}`, then writes promotion judgment to `diagnostic_runs/full_daily_sampler_paired_20260524/paired_promotion_judgment.md` and `analysis/full_daily_sampler_paired_promotion_20260524.md`. Current child work_flow PID=28864; first MLflow experiment=668477595833760942, recorder=3beb9c93c22c467bbda6105b03f992e6. Confirmed resolved config: `train_stop_threshold=None`, `min_epochs=40`, `consecutive_k=999999`, stop=`train_threshold(off, min_epochs=40, k=999999)`. Hook logs: `logs/full_daily_sampler_20260524/paired_hook.stdout.log`, `logs/full_daily_sampler_20260524/paired_hook.stderr.log`. |
| 31 | `result` | full-daily-sampler seed42 pair completed: mechanics fixed but metrics worse | `full_daily` seed42 completed 40e and removed sampler mismatch (`coverage_gap=0`, `duplicate_rate=0`) but underperformed paired `sampled_daily`: RankIC `0.064551` vs `0.073216`, IR_with_cost `1.271` vs `1.441`, MaxDD `-0.0980` vs `-0.0713`, post_peak_decay `0.01271` vs `0.00859`, train-valid gap `0.1330` vs `0.1164`. Hook continues on `sampled_daily` seed43. Cites `diagnostic_runs/full_daily_sampler_paired_20260524/paired_promotion_judgment.json`. |
| 32 | `correction` | row31 used train-loss best, not valid-RankIC best | Paired hook lacked `checkpoint_metric=valid_rank_ic`; stdout restored `loss_main` best. Old hook stopped. Row31 is not a valid promotion judgment. |
| 33 | `sweep_start` | full-daily sampler best-valid paired hook launched | New hook PID=31820; child PID=27512. Adds `checkpoint_metric=valid_rank_ic`, `checkpoint_mode=max`, 40e/no early stop. Out: `diagnostic_runs/full_daily_sampler_paired_bestvalid_20260525`. |
| 34 | `correction` | best-valid hook restarted with gap-at-checkpoint reporting | Restarted hook after adding `curve_summary.rank_ic_gap_at_checkpoint` to reports. New hook PID=31380, child PID=36936. Same out dir and checkpoint policy. |
| 35 | `sweep_done` | full-daily sampler best-valid hook paused by user | Stopped hook PID=31380 and child work_flow PID=36936 before completion. Partial logs in `diagnostic_runs/full_daily_sampler_paired_bestvalid_20260525/full135_full_daily_seed42_ndrop5_40e`. |

## 2026-05-29 (tau-track — IDs continue from 42; the 24-35 block above is the parallel full-daily-sampler track)

| id | kind | key | context |
|----|------|-----|---------|
| 43 | `decision` | extracted router/router_oracle/expert/temporal across all 9 τ-scale arms + ran adversarial-verification workflow | Persisted 177-metric × 9-scale table to `analysis/tau_router_expert_temporal_20260529.csv`. Reference for the new instrumentation groups (expert/oracle/temporal) is det run `689cd5c9` (scale=0.01, RankIC 0.0782) — the ORIGINAL 2026-05-01 baseline lacks these 87→177 row groups. Ran 21-agent workflow (3 lenses × analyze→adversarial-verify): 18 findings, verdicts {confirmed 3, reframed 10, downgraded-to-noise 1, confound-flagged 1, refuted 3}. NO new GPU — pure analysis of existing runs. |
| 44 | `result` | **CORRECTION to Track-5 framing: `router_oracle_gap` is hindsight selection bias, NOT recoverable headroom** | Verified (conf 0.82): `oracle_expert_rank_ic_mean` (0.090-0.133) = per-day hindsight best-of-{time,factor}; `router_oracle_gap` ≡ oracle − default to machine precision (1e-16), and `default` is flat (0.0782-0.0823, std 0.0012) so gap just re-centers the oracle. DECISIVE: `default_beats_oracle_expert_day_ratio` = 0.337-0.488 — the soft mixture out-predicts the hindsight per-day best single expert on 34-49% of 611 days, IMPOSSIBLE for a true achievable ceiling. ⇒ Do NOT target `router_oracle_gap` as Track-5 alpha; a real ceiling needs split-half/OOS oracle to debias the max-of-2 inflation. Supersedes the optimistic oracle-gap reading in row 20 and my prior session message. |
| 45 | `result` | confirmed: factor expert = decorrelation residual; routing mass τ-invariant; time-domination compounds | (R2/E1, conf 0.83/0.90) Forcing time-only (`router_time_advantage` -0.015..-0.040) AND factor-only (`router_factor_advantage` -0.062..-0.098) both HURT at every scale; factor solo RankIC ≈0 (-0.020..+0.019); uniform 50/50 (0.052-0.061) beats time-only at 7/9 scales ⇒ gate value is variance/decorrelation, not winner-picking — KEEP both experts (don't prune factor on solo-IC). (R3/E2, conf 0.90/0.92) Gross routing FLAT-invariant to τ (time_ratio 0.72-0.76, entropy 0.78-0.83, collapse 0); the 'router shifts with τ' story is ONLY correlation-structure (single-seed-suggestive), not routing mass. Effective time-dominance ≈ 0.73 gate × 3.3× intrinsic norm = `contrib_norm_ratio` 11-13× (L0): model far more time-dominated than 0.73 implies — HC-2 soft-mixture realized in degenerate time-dominated form. |
| 46 | `result` | T1 (conf 0.95, source-verified): τ acts ONLY on additive mean-normalized positional embedding, NEVER the attention softmax | `regime_adaptive_embedding.py:94-100` w=exp(-lag/τ), mean-normalized; `quant_moe_model.py:256-261` h += time_emb (broadcast over N). So `attention_*_mass` invariance across all 9 scales (diag 0.12-0.13, local 0.345-0.358, long-range 0.642-0.655) is the EXPECTED signature, not failure. **Reframes the durable 'τ collapse' finding: τ was NOT inherently collapsed — it was clamp-suppressed (`tau_mlp_out_scale=0.01`); unclamping restores strong adaptation (p10-p90 [4.96,4.97]→[3.44,8.91] at scale=5). Retire any 'τ does X to attention' claim.** Also T6 (conf 0.99): `temporal/tau_range_utilization_daily_mean=0` is a within-day-constant artifact (τ broadcast across stocks), NOT failed adaptation. |
| 47 | `correction` | three of my earlier scale=5 / 9-point over-reads REFUTED by adversarial verification | (T5 refuted) `tau_drift=-2.687@scale5` is NOT 'downward drift across 2020→2022' — `work_flow.py:1177` defines it as last_day − first_day, a 2-point endpoint diff ≈1.3 daily-SD of a high-variance 611-day series; it's DISPERSION not trend. My 'τ instability' reading (notebook Entry 4 / prior msg) was reading noise. (T4 refuted) 'extra τ spread unanchored to regime' — `tau_vs_tail_spearman` RISES 0.095→0.176 with spread (Pearson +0.73); τ stays regime-anchored, just shifts axis; regime-homogenization at scale=5 was endpoint cherry-picking (tail_rank_ic_spread vs util Pearson −0.03, min at scale=0.5 not high scale). (E3 refuted) scale=0.8 L1 cosine=−0.11 → IR-dip is causally FALSE: scale=2.0 has same-sign cosine (−0.096) but BEST AnnRet 0.189; the 0.8 IR=1.475 dip has flat RankIC (0.0803) ⇒ single-seed portfolio/turnover noise. **Promotion implication unchanged (reject scale=5 on post_peak_decay/IR), but the mechanistic narrative was wrong.** |
| 48 | `decision` | Track-5 (router-oracle calibration) re-scoped; Phase-2 candidates unchanged (scale 0.5 & 1.0) | Given row 44, Track 5 must NOT chase `router_oracle_gap`. If a router-headroom question is still worth asking, it must use a CAUSAL (non-hindsight) per-day expert selector evaluated OOS, or be dropped. Phase-2 τ multi-seed plan (scale 0.5 & 1.0 × seeds{43-46} + baseline) stands — the workflow corrections don't touch the promotion candidates, only the mechanistic story and the dead Track-5 framing. cites `analysis/tau_router_expert_temporal_20260529.csv`, workflow `wf_3c1b7302-19c`. |

## 2026-05-30 (tau Phase 2 — multi-seed)

| id | kind | key | context |
|----|------|-----|---------|
| 49 | `decision` | Phase 2 redefined by owner: 6 scales × 3 seeds × 15 epochs (faster than 40e/2-candidate plan) | Scales = {0.01(=full_135 τ-frozen control), 0.5, 0.8, 1.0, 1.5, 2.0} — clustered in promotion zone [0.5,2.0] rather than spanning extremes. Rationale: Phase-2 picks a winner among candidates + resolves the scale=0.8 single-seed IR anomaly (1.475) under 3 seeds. Dropped 0.1/5.0: at 15 epochs the overfit signal (post_peak_decay) that differentiated 5.0 won't show (too few post-peak epochs), so extremes lose their differentiator. Seeds {42,43,44}. **Fidelity caveat: 15e < single-seed best-valid epochs (15-19), so post_peak_decay is muted and absolute RankIC will be ~baseline-level; only RELATIVE cross-scale/cross-seed ranking + IR/MaxDD variance is valid.** PARKED: HC-3 "is axis-heterogeneity real when one axis carries 90%+ contribution" — owner links it to future work on reading predictions directly from the time hidden-state (see DESIGN.md/embedding_design.md); deferred until τ track closes. |
| 50 | `sweep_start` | tau_phase2_15e_20260530 — 18-run multi-seed dispatched | 6 settings {full_135, tau_scale_05/08/10/15/20} × seeds {42,43,44}, 15 epochs, deterministic warn, sampled_daily, lr=5e-5, batch=300, amp_fp16, market_state_csi300, checkpoint=valid_rank_ic/max, no early-stop (train_stop_threshold=null, min_epochs=15, consecutive_k=999999). Out: `diagnostic_runs/tau_phase2_15e_20260530/seed{42,43,44}/`. ~18×3hr ≈ 54 GPU-hr expected. Promotion gate per HC-6: 3-seed mean±std on four-tuple, no Tier-S element below baseline mean−1σ. |
| 51 | `correction` | aborted 15e Phase 2 at 4/18 done — 15 epochs too few to show τ effect | Interim seed42 results (full_135/0.5/0.8/1.0 completed) showed 15e truncates below the single-seed best-valid epoch (15-19): RankIC ~0.075 (vs 0.080-0.082 @40e), tau_range_util compressed (scale=1.0 → 0.0079 vs 0.0309 @40e because optimizer hadn't amplified tau_fc2.weight yet), post_peak_decay ≈ 0.0001 (no post-peak window). Owner: re-run at 25 epochs. Killed process tree (bash for-loop PID 6008 + children); background task bwi7feipr failed-by-kill. 15e partial results discarded (cannot mix epoch counts). |
| 52 | `sweep_start` | tau_phase2_25e_20260530 — 18-run multi-seed RELAUNCH at 25 epochs | Same 6 settings × seeds {42,43,44}, now **n_epochs=25, min_epochs=25** (gives a post-peak window so post_peak_decay is meaningful again + lets τ amplify closer to 40e levels, while staying faster than 40e). All other config identical to row 50 (deterministic warn, sampled_daily, lr=5e-5, batch=300, amp_fp16, checkpoint=valid_rank_ic/max, no early-stop). Out: `diagnostic_runs/tau_phase2_25e_20260530/seed{42,43,44}/`. ~18×5hr ≈ 90 GPU-hr expected. Same HC-6 promotion gate. |
| 53 | `sweep_done` | tau_phase2_25e_20260530 complete — 18/18 rc=0 | Full 3-seed × 6-scale table saved `analysis/tau_phase2_25e_3seed_20260604.csv` + PNG. Headline (3-seed mean±std): RankIC ALL arms 0.076-0.077±0.003 (indistinguishable — HC-6 RankIC≠IR re-confirmed). IR_with_cost: 0.01=1.435±0.297, **0.5=1.914±0.236**, 0.8=1.680, 1.0=1.593, 1.5=1.758, 2.0=1.527. seed44 was the hard seed (baseline IR 1.10) dragging all arms. |
| 54 | `result` | PAIRED analysis (same-seed Δ vs baseline) — scale=0.5 is the clear winner | ΔIR paired: **0.5 +0.479 (wins 3/3, t=6.90 df=2, ONLY paired-significant arm)**; 1.5 +0.324 (3/3, t=2.29 NS); 0.8 +0.245 (2/3); 1.0 +0.158 (1/3 — collapses under multi-seed, was my 40e favorite); 2.0 +0.092 (2/3). ΔAnnRet 3/3-sweepers: 0.5 (+0.049), 1.5 (+0.031). ΔRankIC all ≈0. ΔMaxDD all +0.005..+0.013 (all τ arms improve drawdown). 0.5 raises BOTH best-seed (2.246) and worst-seed (1.721) IR above every other arm. Skeptic confirmed: 1.0/2.0 headline gain is mostly "τ rescues hard seed44" (seed44 share 115%/71%); 0.5 is least seed44-dependent (43%, easy-seed mean +0.410). cites `analysis/tau_phase2_25e_3seed_20260604.csv`, workflow `wf_afa32915-371`. |
| 55 | `decision` | tau Phase 2 verdict (4-agent adversarial workflow): **promote scale=0.5 as CONDITIONAL Tier-S, pending Phase-3 walk-forward** | scale=0.5 is the only paired-significant arm on IR (deployment-relevant discriminator); RankIC-neutral, MaxDD-better, AnnRet +34% rel at flat turnover (~0.327, not cost arbitrage). REJECT 1.5 (NS t=2.29, worst MaxDD -0.0807, lowest RankIC; its only edge is the ppd metric being demoted). REJECT hold-for-more-seeds as final action but ADOPT its demand: 0.5 is Tier-S CANDIDATE not production-blessed. **Biggest residual risk: RankIC-flat + turnover-flat ⇒ the IR edge is portfolio-realization/path variance on ONE test split — walk-forward is the kill criterion.** Do NOT set scale=0.5 as code default yet. cites workflow `wf_afa32915-371` final adjudication. |
| 56 | `decision` | HC-6 amendment RECOMMENDED (awaiting owner ratification) — reclassify `post_peak_decay` from hard-veto to non-blocking advisory | Adjudicator: ppd = best_valid_rank_ic − final_valid_rank_ic measures LAST-epoch over-training, but the deployed model is the best-epoch checkpoint (checkpoint_metric=valid_rank_ic/max), so ppd CANNOT touch shipped performance — gating a deployment decision on a train-budget metric is the wrong instrument. scale=0.5 fails strict HC-6 ONLY on ppd (0.00403 vs ceiling 0.00362, a 0.0004 gap). Recommended amendment (must be written GENERALLY for all future arms + logged with deployed-checkpoint rationale, NOT a one-off carve-out for 0.5, else it becomes overfit-laundering): ppd triggers a train-budget action-item (try early-stop / shorter budget), not a promotion veto; keep RankIC/IR/MaxDD as hard gates. **NOT YET APPLIED to hardcore.md — manifesto ratification is owner's call.** |
| 57 | `decision` | **HC-6 amendment RATIFIED by owner (2026-06-04): `post_peak_decay` → non-blocking advisory** | Owner approved. Applied to `shared/hardcore.md` (Core amendment 2026-06-04). ppd no longer a promotion veto; it raises a logged train-budget action-item. Hard gates remain RankIC/IR/MaxDD (deployment-relevant). Anti-laundering: written generally for ALL arms + VOID if deploy ever stops using best-epoch checkpoint. Under amended gate, scale=0.5 passes all hard gates → promoted to **conditional Tier-S** (logged ppd advisory: evaluate early-stop/shorter budget for scale=0.5). Still NOT code default, NOT production — pending Phase-3 walk-forward. |
| 58 | `decision` | owner chose: ADD SEEDS to consolidate 0.5 before walk-forward (not straight to Phase 3, not HC-3 thread) | Run scale=0.5 + baseline (full_135) at 3 additional seeds {45,46,47} → n=6 each. Goal: shrink the IR noise band (baseline sample-std 0.36 at n=3) and confirm the 3/3 paired-win pattern survives (kill check: does any new seed break it?). 25 epochs, same protocol. Then Phase-3 walk-forward. HC-3/hidden-state thread remains parked. |
| 59 | `sweep_start` | tau_phase2_seedext_20260604 — 6-run seed extension dispatched | full_135 + tau_scale_05 × seeds {45,46,47}, 25 epochs, deterministic warn, sampled_daily, identical protocol to tau_phase2_25e. Out: `diagnostic_runs/tau_phase2_seedext_20260604/seed{45,46,47}/`. ~6×5hr ≈ 30 GPU-hr. On completion: combine with rows 53-54 (seeds 42-44) → n=6 paired analysis for scale=0.5 vs baseline; confirm/refute 3/3→6/6 paired-IR-win pattern before Phase-3 walk-forward. |

## 2026-06-06 (tau Phase-2 KILL-CHECK — promotion reversed)

| id | kind | key | context |
|----|------|-----|---------|
| 60 | `sweep_done` | tau_phase2_seedext_20260604 complete — 6/6 rc=0, config parity verified | full_135 + tau_scale_05 × seeds {45,46,47}, identical config to seeds 42-44 (verified: only seed differs). Combined n=6 table `analysis/tau_0p5_vs_baseline_n6_20260606.csv`. |
| 61 | `result` | **n=6 KILL-CHECK: scale=0.5 IR edge LOST significance** | Per-seed ΔIR (0.5−baseline): s42 +0.422, s43 +0.397, s44 +0.617, s45 +0.030 (all WIN) → s46 **−0.176**, s47 **−0.404** (LOSS). Paired ΔIR collapsed n=3 +0.479 (t=6.90) → **n=6 +0.148 (t=0.92, NS; t-crit .05=2.015)**, wins 4/6. The 3 fresh seeds ALONE are net −0.183 (win 1/3). Leave-one-out drop-s42 → +0.093 (t=0.50). ΔAnnRet +0.016 (t=0.95 NS). ΔRankIC −0.0006 (flat-neg). **Only survivor: ΔMaxDD +0.0102 (t=2.09, barely over crit, multiplicity-uncorrected, single-split-fragile).** τ adaptation still mechanically real (tau_util 0.0104 vs 0.0002, 50×) but does NOT yield a seed-robust portfolio edge. baseline s44 IR=1.10 was a low outlier → the celebrated "seed44 rescue" was largely regression-to-mean of an unlucky baseline draw. cites workflow `wf_31b7244d-2d8`. |
| 62 | `decision` | **REVERSE scale=0.5 promotion (conditional Tier-S → baseline); τ IR-edge = NULL at n=6** | 2-advocate adversarial workflow + adjudicator (`wf_31b7244d-2d8`): a conditional promotion whose PRIMARY metric (IR) fails its own pre-registered kill-check has failed. scale=0.5 demoted out of Tier-S. No code default change was ever made (default stays 0.01) — so "revert" = record de-promotion; deployed config unchanged. MaxDD/variance-reduction survivor NOT a basis for any tier (t=2.09 marginal, F-tests NS: IR F=1.51, MaxDD F=3.84 << Fcrit 7.15, uncorrected across 5 metrics, single-split). **KEEP the mechanistic finding (tau_util 50× real); ADD durable finding "τ-unclamp IR-edge = null at n=6" (mechanism-real ≠ portfolio-benefit).** Do NOT spend walk-forward/stress compute on 0.5 — wasted on a non-effect. τ-as-portfolio-lever = UNDETERMINED (not dead). **DISCIPLINE SUCCESS: the kill-check (owner's "add seeds first" call) caught a 3-seed sampling fluctuation before walk-forward.** |
| 63 | `decision` | HC-6 rollback (ppd advisory → hard gate) RECOMMENDED — awaiting owner (reverses row-57 ratification) | Adjudicator: the ppd→advisory amendment (Core amendment 2026-06-04, ratified row 57) was triggered by the now-dead 0.5 IR promotion; the disciplined move is to restore ppd as a hard gate so the manifesto isn't left with a data-contingent change made under promotion pressure (the "cherry-pick optics" the skeptic flagged). NOTE the instrument-validity argument (ppd measures last-epoch overtraining, deploy uses best-epoch checkpoint) is independent of 0.5 and could stand on its own merit — so this is a genuine owner judgment: roll back to original gate, or keep ppd-advisory on standalone merit. NOT YET APPLIED to hardcore.md. |
| 64 | `decision` | owner KEEPS HC-6 ppd→advisory (does NOT roll back) — retained on standalone instrument-validity merit | Owner chose to retain the amendment despite scale=0.5's reversal: the rationale (ppd measures last-epoch over-training; deployed model is best-epoch checkpoint; ppd can't touch shipped perf) is independent of any specific promotion. Added Addendum 2026-06-06 to `shared/hardcore.md` noting 0.5 was reversed but the amendment stands; self-voiding clause (revert if deploy stops using best-epoch checkpoint) remains in force. Hard gates: RankIC/IR/MaxDD. |
| 65 | `decision` | τ track CLOSED as alpha lever; returning to main-board bottlenecks | Owner: wrap τ (keep mechanistic finding + n=6 IR-null), do NOT spend walk-forward on 0.5. HC-3/hidden-state thread stays PARKED. Re-prioritize untouched/highest-ROI bottlenecks (re-weighting delegated to claude). τ-as-risk-lever (drawdown/variance) remains UNDETERMINED — only pursue via pre-registered drawdown criterion across scales {0.05,0.1,0.25} if ever budgeted, not now. |
| 66 | `decision` | bottleneck re-prioritization → next target = pool collapse (Track 1.5, `pool-forensics-20260524.md`) | Re-weighed all bottlenecks: gen-gap (capacity+τ+sampler-invariant — full_daily made it WORSE per Track-4 rows 24-35; only EMA-rampup untested, but resisted everything = low EV); router-cal DEAD (oracle=hindsight); temporal-via-τ DEAD (no portfolio benefit); capacity low-EV. **Pool collapse chosen: biggest untapped ARCHITECTURAL lever** — factor expert vestigial (contrib 11:1, solo RankIC≈0, six-nines pool_entropy=1.0, pool_top10≈1/N). High-info regardless: un-collapse+RankIC-move = real gain; un-collapse+RankIC-flat = factor axis genuinely signal-less → feeds parked HC-3/hidden-state decision. τ-lesson applied: cheap test first. Plumbed `pool_n_heads` through config (`model_configuration.py:70,177` + `quant_moe_model.py:108`, default 1 unchanged; smoke-tested). |
| 67 | `sweep_start` | pool_forensics_20260606 R1+R2 dispatched (seed 42, 25e) | 2 arms vs full_135 baseline (25e seed42, pool_entropy=1.0/pool_top10=0.063): **R2** `pooling_alpha=1.0` (remove 0.3 mean-blend bypass, pure override); **R1** `pool_n_heads=4` (break single-query symmetry, plumbed). Decision (per card): un-collapse iff `pool_entropy_norm_mean<0.95` AND `pool_top10_mass≥0.15`, with four-tuple no-regression (HC-6 hard gates RankIC/IR/MaxDD). Out: `diagnostic_runs/pool_forensics_20260606/seed42/`. ~12 GPU-hr. R3 (logit-temperature, needs code change) deferred pending R1/R2 outcome. |

## 2026-06-06 (pool-readout GATE — cheap pre-screen for pool-forensics R1/R2/R3)

| id | kind | key | context |
|----|------|-----|---------|
| 68 | `sweep_start` | pool-readout-gate-20260606 dispatched (KEY-SVD + PROBE-CEILING, seeds 42/43/44) | Cheap mostly-inference gate that decides whether pool-forensics R1/R2/R3 (id 67, started 10:22 then KILLED after 1 step — no results) can work AT ALL, BEFORE re-spending GPU. Reframe (code-verified `attention_pooling.py:71-86`): `pool_entropy_norm=0.99999` measures only attention WEIGHTS; at uniform weights the pool is still `LayerNorm(W_o W_v mean(h))`, a learned recoloring the metric is BLIND to — "six-nines" ≠ "pool dead". Three worlds: A operator-state-collapse (keys vary, query cold → fixable), B input-forced (keys ~constant → no operator helps), C live-projection-mean (mean is ~IC-optimal → sharpening futile). **KEY-SVD** (per-sample SVD of centered cross-factor keys; world-B indicator `s1_over_common`; temperature-peakability sweep decoupled from the cold query) separates B from (A or C). **PROBE-CEILING** (frozen backbone; Arm0 LN-mean-pool ridge vs ArmA capacity-matched convex-reweight; Arm B 158 marginal slot-ICs) separates A from C. Decision: world B iff `s1_over_common≤0.05`; sharpening_can_help iff ArmA−Arm0≥+0.003 on seed-majority AND slot decile≥2 AND cov-effrank≥2. Faithfulness gate valid_rank_ic∈[0.072,0.082]. Forensic NOT promotion (no DSR budget). n=3 caveat per id 61-62 (τ kill-check). Impl `scripts/pool_readout_forensics.py` + `scripts/aggregate_pool_forensics.py`; pre-registered `shared/falsifiers/pool-readout-gate-20260606.md`; SOTA grounding workflow `wf_bfd1a207-f10`. Smoke validated: forensic valid_rank_ic 0.05825 == model Valid-01 0.05814 (faithful capture). Out: `analysis/pool_readout_forensics/seed{42,43,44}/`. ~1.5-2 GPU-hr (3 backbones + inference). |
| 69 | `sweep_done` | pool-readout-gate-20260606 complete — n=6×2 on LOADED backbones (no retraining) | Scope UPGRADED from the filing: instead of training, found faithful full_135 (τ-frozen) AND tau_scale_05 saved backbones for seeds 42-47 in MLflow (`R.save_objects(model=model)`, `work_flow.py:2735`) — loaded via pickle (patched stale `_market_state` wrapper attr). 12 backbones × (KEY-SVD + PROBE), fp32, forward_pre_hook on `net.factor_pooling`. Faithfulness PASS: fp32 valid rank_ic 0.0781±0.0027 (full_135) / 0.0775±0.0033 (tau05) == saved MLflow. Adversarial-verify workflow `wf_0d6432f7-af6` (conf 0.74) caught the original PROBE as OVER-SCOPED (tested only static-global reweight) → added **ArmA′ input-conditional query (the R1/R3 function class)**, **ArmC PMA-k4**, **Arm-ASP 2nd-moment**, and **hold-selected ArmA** (fixed a datetime64 hold-split bug that had silently emptied the CV split). Out: `analysis/pool_readout_forensics/REPORT.md` + `combined.json`. |
| 70 | `result` | KEY-SVD: collapse is OPERATOR-STATE not input-forced; PROBE: ALL factor-pool operators parity-or-worse | KEY-SVD (n=6×2): `key_s1_over_common` 2.17/2.90 (>>0.05 ⇒ NOT world B), eff-rank 4.2/2.8, peakable (entropy@logit-std3 0.66/0.71); but learned `‖q_proj‖≈0.01`, logit-std≈0.02, cross-stock weight-std≈5e-5 ⇒ a COLD near-input-independent query is the cause of the six-nines, not dead inputs. PROBE gaps vs Arm0 (LN-mean, hold-selected, +0.003 gate): static −0.0009/0.0000 (0/6,0/6); **conditional ArmA′ −0.0007/−0.0031 (0/6,0/6)**; PMA-k4 −0.0024/−0.0009 (0/6,0/6); ASP +0.0003/+0.0002 (1/6,1/6). ArmB slot-IC decile 1.45/1.40, max|slot-IC|≈0.074≈full-model. τ-invariant. |
| 71 | `decision` | **settled-P2-fires: factor-pool sharpening is a DEAD RankIC lever; do NOT re-dispatch pool-forensics R1/R2/R3** | World C confirmed at full strength on the operative function class: equal-weight LN-mean pooling is ~IC-optimal; the 158 factors are ≈equally-weak & redundant. R1 (multihead) / R2 (alpha=1.0) / R3 (temperature) would un-collapse `pool_entropy` (mechanically possible — keys peakable) but leave RankIC flat. **Park pool-forensics-20260524 R1/R2/R3 for RankIC** (do not re-dispatch id 67). Routes to the parked HC-3 / read-from-time-hidden-state decision: the alpha is time-dominated, Stage-2 factor-attention is signal-less at readout. SCOPE: proven csi300 t+5 2020-2022 n=6×2 (seed-robust not regime-robust; csi800 untested); ASP 2nd-moment is a separate lever (also flat, would need own card). Settles `shared/falsifiers/pool-readout-gate-20260606.md` → `settled-P2-fires`. |

## 2026-06-06 (time-axis readout — headroom redirected from the dead factor pool)

| id | kind | key | context |
|----|------|-----|---------|
| 72 | `sweep_start` | time-readout-bonus-20260607 dispatched (overnight autonomous, Step-A screen) | After pool settled-dead (id 71), headroom redirected to the TIME readout. Q: does a learned-over-T readout beat last-step `h[:,-1]`? CONFOUND: model trained on last-step → earlier steps get no direct readout grad → "last-step highest marginal IC" is expected, NOT optimality; frozen probe = confounded lower bound. Designs (mean_N collapse, end [B,D]→head): D6(mean replaces dead pool), D1-PMA(gated-residual attn-over-T), D1-dual-a(concat[time-attn, global-mean]), D1-dual-b(concat[time-attn, attn-over-N]), D3-CI-D(per-D linear over T), D3-CI-N(per-factor linear over T). **Identity-start CORRECTION (owner-caught):** softmax one-hot `b_t[last]=+20` SATURATES (off-last grad ≈e^-20≈2e-9 → gradient-lock = mirror of six-nines → false null) → use GATED-RESIDUAL `(1-g)·last + g·attn`, gate=0(g=0.5 responsive), b_t=0; linear (D3) keeps one-hot-last (no saturation). Base = scale=0.5 backbones seeds 42-47 (n=6). Two-step: Step-A frozen+cached screen (~1-2 GPU-hr, escalation gate +0.003, never-kill) → Step-B readout+top-block continue-finetune for winner (HC-6 four-tuple, ~4 GPU-hr). D2 dropped (no new time hyperparam), D5 deferred (use_alibi=False confound). Pre-reg `shared/falsifiers/time-readout-bonus-20260607.md`; plan `analysis/readout_redesign/PLAN_overnight.md`; design doc `analysis/readout_redesign/DESIGN_D1-D6.md` (workflow wf_e9bdcfec-fba); impl `scripts/time_readout_finetune.py`. |

| 73 | `sweep_done` | time-readout-bonus-20260607 complete — Step-A(n=6) + Step-B(n=3), scale=0.5, serial overnight | Master ran ~10h serial (Step-A frozen screen all 6 seeds + Step-B control/d3cid finetune × seeds 42-44), no concurrency. Engineering: caught+fixed 3 issues via smoke — (1) concurrency crash (2 qlib procs fought joblib temp dir → pinned TMP to repo tmp_run, full serialization); (2) identity-start bug (replacing pool mismatched the loaded head, step0 0.061≠0.081) → fixed by temporal-aggregation-BEFORE-pool (tr_A[T,D] one-hot-last ⇒ z=h[:,-1] ⇒ step0==0.0811==model exact); (3) softmax saturation (owner-caught +20 one-hot) → gated-residual. Impl `scripts/time_readout_finetune.py` (Step-A), `time_readout_stepB.py` (Step-B), model edit `quant_moe_model.py:400-410` (guarded temporal_readout flag, default unchanged). Out `analysis/readout_redesign/RESULT_overnight.md`. |
| 74 | `result`+`decision` | **time readout NOT a RankIC lever; readout layer (factor+time) fully settled; redirect to gen-gap** | **P1 fires** (Step-A n=6): no design beats frozen last-step baseline on seed-majority; per-seed bonus means d1pma −0.0018 / duala −0.0034 / dualb −0.0056 / **d3cid −0.0006 (best, 1/6)**; d3cid seed-42 +0.0054 did NOT replicate (−0.006@45) = n=1→n=6 reversal. **P2 fires scoped** (Step-B n=3): d3cid≡control to ~1e-4 (diff 0.00000); (a) continue-finetune HURTS valid every epoch (0.0811→0.078) BOTH arms = overfits = **gen-gap finding #2 re-confirmed** (best=step0); (b) tr_A stuck at one-hot-last (W_last_frac 0.95, no gradient pull off last-step). CAVEAT: finetune overfits-not-explores → from-scratch d3cid (Tier-B-gold) untested. **DECISION: readout layer (factor pool dead id71 + temporal readout ~optimal here) is NOT the lever → stop readout-architecture work; route to gen-gap track (sampler-dedup/EMA-rampup, the honest untested levers per finding #2). From-scratch d3cid only if gen-gap stalls.** Settles `shared/falsifiers/time-readout-bonus-20260607.md` → settled-P1+P2-fire(scoped). |

---

*— end of ledger header —*

## 2026-06-07

| id | kind | key | context |
|----|------|-----|---------|
| 75 | `decision` | fix `qx-skill-creator` new-skill template YAML | Quoted templated frontmatter in `templates/new_skill/SKILL.md` after Codex skipped it as invalid YAML. |

## 2026-06-12

| id | kind | key | context |
|----|------|-----|---------|
| 76 | `result` | from-scratch readout matrix (6 designs, seed42, scale=0.5, 25ep) = NULL — retires the P2 "overfits-not-explores" caveat | ΔRankIC vs control(0.08107): d3cid −0.0003, d3cin −0.0017 (clean re-run after 2 external-CUDA kills; portfolio collapsed: IR 0.54 vs 2.25, MaxDD −0.41 vs −0.06 = finding-#3 amplified), d3mix +0.0002 (best, noise), d1pma −0.0034, duala −0.0060, dualb −0.0093; ALL 6 portfolio-worse. From-scratch DOES explore (tr_W_last_frac 1.0→0.906; d3mix mixing gains 0.13/0.19 = capacity activated for zero RankIC — mechanism≠benefit replayed). Impl `run_readout_matrix_scale05.sh`; report `analysis/readout_redesign/readout_matrix_RESULT.md`. Pre-launch 17-agent adversarial review found 9 bugs (0 run-corrupting; the valid-vs-test ΔRankIC fallback fix mattered — it was live-triggered). |
| 77 | `result`+`decision` | **init-ablation falsifies the basin-trap hypothesis; gate-frozen mechanism; temporal-readout axis CLOSED → card settled-CLOSED** | Paired A/B, byte-identical backbones (separate-RNG init): uniform_mean (1/T+0.02·randn, sum-preserving NOT Kaiming) CLEARLY worse on all 3 linears (d3cid −0.0137, d3cin −0.0131, d3mix −0.0088) and never travels back (last_frac 0.125→0.13–0.15, best_ep 16 not censored) ⇒ **one-hot-last does NOT trap — the uniform basin IS the trap; last-step is the attractor**. d1pma gate sweep: tr_gate FROZEN at init (0.12→0.124, 0.5→0.503, 0.88→0.874 — scalar sigmoid gate gets ~no gradient); rank_ic g0.12 0.08163 (≈control +0.0006) > g0.88 0.07848 > g0.5 0.07765 ⇒ pre-registered clause-b fired (+0.0040≥+0.003): the attentions' matrix deficit was the FORCED 50% mixture (init artifact); best case = parity, never a gain. **DECISION: readout axis closed in all 5 regimes (frozen n=6 / finetune n=3 / from-scratch ×6 / init A/B ×3 / gate ×3); no promotion candidate; route stays gen-gap + h-20260610 cards. Engineering rule: never init a scalar sigmoid gate at 0.5 expecting training to choose — it keeps its init.** Runner self-healed via GPU-precheck+retry (6/6 first-attempt after 2 prior kills). Impl `run_init_ablation_scale05.sh`+`compare_init_ablation.py`; report `init_ablation_RESULT.md`; settles `shared/falsifiers/time-readout-bonus-20260607.md`. |
| 78 | `result`+`decision` | **g0.12 d1pma fresh-seed n=3 = KILL (paired ΔRankIC +0.00099±0.00503, sign-flips) — attention readout closed; card → settled-CLOSED-KILLED, zero residuals** | Owner-requested kill-check of the only above-control point (seed42 +0.0006). Per-seed paired ΔRankIC: +0.0006(s42) / −0.0038(s43) / +0.0062(s44) — sign flips = sampling noise around parity, the exact τ-episode n=1 mirage pattern; ΔIR incoherent (−0.71/+0.32/−0.10). Gate frozen at init in ALL runs (0.124/0.125/0.124 from 0.119) — scalar-sigmoid-gate-keeps-its-init now n=3-confirmed. Pre-registered kill line (mean<+0.003) fired with no goalpost-moving. **Temporal-readout axis exhausted across 6 regimes; the kill-check discipline (memory: n=3 fresh-seed before believing any n=1 point) caught its 3rd mirage.** Report `analysis/readout_redesign/g012_n3_RESULT.md`. |
| 79 | `decision` | **BASELINE ANCHOR moved → τ=0.5 + d1pma g0.12 (owner designation, anchor-at-parity NOT promotion)** | Future experiments compare vs the 3 g012 runs (new CTRL: s42 `mlruns/716849652326531066/2e8e24fe...` 0.08163, s43 `mlruns/686003645691947084/500a105a...` 0.07500, s44 `mlruns/520724743245902018/79af86fa...` 0.07857; mean 0.07840 std 0.0033 vs old control 0.07741 std 0.0044). Rationale: dormant-attention platform value + suggestive variance reduction; kill verdict (row 78) STANDS — no edge claimed. Reversible: unset `temporal_readout` ⇒ old control byte-identical. Archive: `analysis/readout_redesign/archive_g012_baseline/BASELINE_ANCHOR.md` (settings, code flow, manifest, logs, resolved confs); reproducible env `scripts/baseline_g012_scale05.env.sh`. Old anchor mlruns kept. |
