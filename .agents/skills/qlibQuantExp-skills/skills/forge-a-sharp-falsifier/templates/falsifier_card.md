# Falsifier Card — {{claim short title}}

**claim_id:** `{{kebab-case-id-YYYYMMDD}}`
**filed_at:** {{YYYY-MM-DD}}
**filed_by:** {{author / context}}
**parent_baseline_sha:** {{anchor sha from ledger}}
**status:** open | settled-pass | settled-fail | settled-partial

---

## The claim — stripped of hedging

{{One sentence. No "we think" or "may". State the claim as a binding prediction.}}

## The prohibition (what this claim FORBIDS)

| Prohibition | What its failure means |
|---|---|
| **P1.** {{prohibition statement}} | {{interpretation if violated}} |
| **P2.** {{prohibition statement}} | {{interpretation if violated}} |

## Ex-ante metric specification (binding)

| Field | Value |
|---|---|
| `universe` | {{e.g. csi300 / csi800}} |
| `horizon` | {{e.g. t+5}} |
| `metric` | {{exact metric name, e.g. rank_ic_daily mean}} |
| `window` | {{exact start..end date range}} |
| `seeds` | {{exact list, e.g. [42, 43, 44, 45, 46]}} |
| `decision_rule` | {{precise: delta ≥ X, on ≥ N/M seeds, etc.}} |
| `null_hypothesis` | {{delta ≤ 0 or equivalent}} |
| `n_trials_at_filing` | {{read from shared/ledger.md}} |
| `multiple_testing_note` | {{any haircut considerations}} |

## Anti-rescue rules (binding)

1. **No segment surgery.** Cannot redefine the test window after seeing results.
2. **No metric swap.** Cannot switch the decision rule to a different metric.
3. **No seed selection.** All seeds must be reported; cannot drop "outliers".
4. **No subgroup mining.** Cannot claim victory on a subset if the overall test failed.
5. **No reinterpretation.** Cannot retroactively say "we were testing X" when the card says "Y".
6. **No arm-set surgery.** All arms must complete; cannot drop arms after seeing results.
7. **No threshold relaxation in this filing.** File a new card with new claim_id for a relaxed threshold.
8. **No hidden hyperparameter sweep.** Pre-commit to specific values; or pre-commit to a sweep and report all.

Any rescue requires filing a **new** falsifier card with new `claim_id` and the rescue admitted explicitly in `motivation`.

## Settlement protocol

When sweep completes:
1. `qx-research-ledger` appends `sweep_done` rows.
2. Compute results. Apply decision rule **as written above**.
3. Append a `kind=result` row and a `kind=decision` row to `shared/ledger.md` citing this file.
4. Update this card's `status` field to `settled-pass` / `settled-fail` / `settled-partial`.
5. If the result touches a hard-core item, trigger `qx-protect-or-relax-the-hard-core`.

---

*Generated by `qx-forge-a-sharp-falsifier`. Save as `shared/falsifiers/<claim_id>.md`. Never edit prior content; only append a `## Settlement YYYY-MM-DD` block at the bottom.*
